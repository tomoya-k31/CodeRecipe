<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.tomoya.asyncsqlite.app" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:name="me.tomoya.asyncsqlite.app.GlobalApplication">
        <activity
            android:name="me.tomoya.asyncsqlite.ui.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ActiveAndroid -->
        <meta-data android:name="AA_DB_NAME" android:value="sample.db" />
        <meta-data android:name="AA_DB_VERSION" android:value="1" />
        <meta-data android:name="AA_SERIALIZERS" android:value="me.tomoya.asyncsqlite.db.UtilDateSerializer" />
    </application>

</manifest>
